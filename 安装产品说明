一键化安装部署
步骤：
环境检查：
-- 新机房建设的同城备和异地备需要开通防火墙权限 -- 提前提防火墙工单
-- 自动托管服务器需要服务器密码   -- 待确认
-- precheck;  服务器规格，磁盘大小

1、纳管服务器   
2、安装数据库	
	-- 不支持安装insight
	-- 批量安装流程调整，新机房安装空insight
		-- 批量初始化环境
		-- 然后批量
	-- 自动识别配置模版  			-- 手动指定 
	-- 3306改透传
	-- 初始化管理权限   --工具
	-- 物理机绑核（CN，DN）  --  准备脚本
	-- 配置备份任务
		--安装GFS
		--配置备份任务    --手动，尝试用脚本
3、初始化通用用户的权限 
4、安装监控   -- 
5、配置备份任务
	-- 提前安装gfs      
	-- 可以调用接口部署任务      	--手动
	-- clouddb资源池维护   --   
厂商操作：
	-- 3307大数据CN资源限制调整   	--手动
		-- 新增4C16G组件规格
		-- 虚拟机2c4g

		-- 配置组件资源限制
	-- 调整高低水位   				--手动
	-- 配置不跨AZ切换，致性优先 		--手动
	-- 配置备库延时时间 			--手动
	-- 3307 配置读写分离（可读主库、物理机）
	-- 3308，3309配置读写分离（不可读主库、同城备、异地备）

6、初始化表结构 -- 可选 

7、初始化权限   --可选
	-- 白名单
		-- 应用IP对比问题 -- 
		-- 检查问题
	-- ？  权限合并问题

